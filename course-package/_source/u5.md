# u5｜React CRM 工作台與前後端整合

> 建立前端 CRM 工作台，對接登入與客戶列表、詳情、生意機會看板，並把總覽做成含漏斗、營收預測、風險分布等分析圖表與客戶分群的經營儀表板。

**時數**：13:00 ~ 17:00

**特色**：掌握 Node.js 環境、使用 Vite 建立 React 19 專案、JSX 語法元件結構，並學習如何以 Proxy 串接後端 API 及套用 uiuxpromax 優化前端視覺體驗。

## 學習目標
- 理解 Node.js 與 NPM 相依性套件管理機制
- 學會使用 Vite 初始化 React 19 專案的指令步驟
- 掌握 JSX 語法特色與 Functional Component 元件結構
- 理解為何需要開發代理 (Vite Proxy) 以免除 CORS 限制
- 掌握 uiuxpromax 視覺優化要點（毛玻璃、漸層Header、微動畫與骨架屏）

## 核心原則
前後端整合的核心在於狀態的一致性與防禦性渲染。Axios Interceptor 可以自動為請求附加 JWT，並在 401 時引導重新登入；UI 上必須對 Loading、Error 及 Empty 進行完整處理，保障使用者體驗。

## 概念講解（concepts）

### Node.js 與 React 專案建立（分組：Node.js 與 React 專案建立）
Node.js 是前端開發的執行環境，而 NPM (Node Package Manager) 是套件管理工具。在 React 19 的開發中，我們不再使用傳統手動下載 JS 檔的方式，而是使用 NPM 安裝相依套件。

我們使用業界主流、極速的 Vite 作為建置工具，透過以下指令在命令列中建立專案：

- 建立 Vite React 專案：`npx create-vite@latest frontend --template react`
- 進入專案目錄：`cd frontend`
- 安裝最新無資安漏洞的 React 19 依賴：`npm install`
- 啟動本機開發伺服器 (Port 5173)：`npm run dev`

### JSX 語法與 Functional Component 元件結構（分組：JSX 語法與元件結構）
JSX 是 JavaScript 的語法擴充，允許我們在 JavaScript 中直接撰寫一種類似 HTML 的結構。React 19 推薦使用 Functional Component（函式元件）進行開發，相比舊版的 Class 元件更加簡潔。

JSX 寫作規範與注意事項：

1. 所有元件必須回傳「單一根節點」（若有多個元素，需用空標籤 `<></>` 包裹）。

2. 由於 class 在 JS 中是保留字，JSX 中必須改寫為 `className`。

3. HTML 事件綁定需改為 React 的小駝峰命名（例如 `onclick` 改為 `onClick`）。

4. 變數與邏輯表達式可直接放在大括號 `{}` 中進行求值與渲染。

**Counter.jsx — 基本 JSX 函式元件範例 (jsx)**
```jsx
import React, { useState } from 'react';

// 宣告一個 Functional Component 元件
export default function Counter({ initialCount = 0 }) {
  // 使用 useState Hook 管理元件內部的狀態
  const [count, setCount] = useState(initialCount);

  return (
    <div className="counter-container">
      <h3>當前計數器：{count}</h3>
      {/* 點擊事件小駝峰命名，並使用大括號綁定 JavaScript 方法 */}
      <button onClick={() => setCount(count + 1)}>
        累加 +1
      </button>
    </div>
  );
}
```

### 開發端代理與後端 API 串接 (Vite Proxy)（分組：Vite Proxy 前後端串接）
在前後端分離的架構中，前端 Vite 伺服器運行在 `http://localhost:5173`，而 Spring Boot 後端運行在 `http://localhost:8080`。如果前端直接向後端發送非同步請求 (Fetch / EventSource)，會因為「同源政策 (Same-Origin Policy)」而被瀏覽器阻擋 (CORS 跨網域錯誤)。

為了解決此問題，我們在 `vite.config.js` 中配置 `server.proxy` 代理轉發，將所有以 `/api` 開頭的請求，在開發環境中自動轉發至 `http://localhost:8080`。這樣前端程式碼只需填寫相對路徑即可，且完全免除了後端配置 CORS 的繁瑣設定！

**vite.config.js — 配置 API 代理轉發 (javascript)**
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // 當前端請求 /api/ai/stream 時，Vite 自動代理為 http://localhost:8080/api/ai/stream
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

### uiuxpromax 前端視覺優化指引（分組：前端視覺優化指引）
一個優秀的 Web 應用不僅要能跑，更要能 WOW 使用者。在智慧客服專案中，我們採用 uiuxpromax 的設計哲學，利用 Vanilla CSS 優化整體視覺，徹底告別單調的 MVP 樣式：

- 🎨 毛玻璃效果 (Glassmorphism)：卡片使用 `backdrop-filter: blur(14px)` 搭配半透明邊框，營造精緻浮空感。
- 🌈 漸層極光配色：Header 使用 linear-gradient(135deg, indigo, purple) 漸層配色，搭配狀態指示燈 (Pulse LED) 動態閃爍。
- ⚡ 微懸停動畫 (Micro-interactions)：滑鼠懸停於客戶摘要、待辦任務卡片時，加入 `transform: translateY(-4px) scale(1.01)` 與 `transition` 讓卡片活起來。
- ⏳ 骨架屏載入動畫 (Skeleton Screen)：當 AI 正在思考或呼叫 Tool 時，在對話框中顯示灰白色的骨架屏閃爍 (shimmer keyframe)，極大降地等待期間 of 無聊感。

---

📦 **uiuxpromax 的「安裝」與配置方式**

`uiuxpromax` 並非傳統的 npm 第三方套件，因此不需要執行 `npm install`。它的「安裝與引入方式」是將以下精心調校的 Vanilla CSS 樣式直接整合進 React 專案的 `src/index.css`（或 `App.css`）中，讓元件直接透過 `className` 引用：

```css
/* uiuxpromax 核心樣式配置 */
.glass-card {
  background: rgba(255, 255, 255, 0.45);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  border: 1px solid rgba(255, 255, 255, 0.25);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.07);
}

.gradient-header {
  background: linear-gradient(135deg, #4f46e5, #9333ea);
}

.micro-interaction {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.micro-interaction:hover {
  transform: translateY(-4px) scale(1.01);
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
}

@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
.skeleton-shimmer {
  background: linear-gradient(90deg, #f3f4f6 25%, #e5e7eb 50%, #f3f4f6 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite linear;
}
```

### AI Agent 提示詞 — 建立 React 前端專案（分組：AI 提示詞 — React 前端）
理解 React 專案結構、Proxy 原理與視覺優化要點後，複製以下提示詞給 AI Agent，從零建立聊天室的前端專案骨架。

**AI Agent 提示詞 (text)**
```text
請幫我建立 CRM 智慧工作台的 React 前端專案：
1. 在專案根目錄用 Vite 建立 React 19 專案：npx create-vite@latest frontend --template react，並執行 npm install。
2. 設定 vite.config.js 的 server.proxy，把所有 /api 開頭的請求代理到 http://localhost:8080，解決前後端分離的 CORS 問題。
3. 建立 App 頁面骨架：包含漸層色 Header（linear-gradient 靛藍到紫色）、毛玻璃卡片容器（backdrop-filter: blur），以及聊天視窗的占位區塊。
4. CSS 請加上骨架屏（skeleton shimmer）載入動畫，之後聊天室等待 AI 回覆時會用到。
5. 元件需有中文註解。
完成後請告訴我如何啟動開發伺服器，以及如何確認 proxy 代理有生效。
```

### CRM 工作台 UI 設計思維（分組：CRM 前端架構）
CRM 前端工作台的核心畫面設計，圍繞業務人員的日常工作流：

**Dashboard（儀表板）**
展示關鍵指標：本月商機總額、待跟進客戶數、即將到期合約、AI 建議的優先行動。使用卡片式佈局，數據來自後端 API 的即時聚合。

**Customer List（客戶列表）**
支援動態篩選（產業、合約狀態、最後互動時間）與搜尋。列表使用虛擬滾動處理大量資料，點擊進入客戶詳細頁面。

**Opportunity Board（商機看板）**
看板式佈局（類似 Trello），每個欄位代表商機階段（Prospecting → Qualification → Proposal → Closing）。可拖拽商機卡片變更階段。

**AI Chat（AI 助理）**
右側浮動面板或獨立頁面，支援 SSE 串流對話、Markdown 渲染、客戶摘要卡片與行動建議按鈕。

**元件設計原則**
- 每個頁面是一個 Route，共用 Layout 元件（側欄 + 頂部導覽）
- 資料請求統一用 Axios + Vite Proxy
- CSS 使用 HSL 色彩系統，支援深色/淺色主題切換

## 提示詞（prompts）

### ① 做出網頁畫面的外觀骨架［build］
> 先把整體版面和現代感的視覺架好
```text
請幫我做出這套系統的網頁畫面外觀：要有現代感的設計（例如漸層的標題列、卡片式的區塊、載入時有過場動畫），先把整體版面骨架架好，內容之後再填。請加中文註解，完成後告訴我怎麼打開來看。
```

### ② 讓網頁接上後端並記住登入［build］
> 登入後自動記住身分，過期自動回登入頁
```text
請幫網頁接上後端：登入成功後自動記住我的身分，之後每次操作都自動帶著；如果登入過期了，就自動把我導回登入頁重新登入。請加中文註解。
```

### ③ 做出 CRM 的核心頁面並接上真實資料［build］
> 登入 / 總覽 / 客戶清單 / 客戶詳情 / 生意機會看板
```text
請做出 CRM 的幾個主要頁面，並接上後端的真實資料：登入頁、總覽儀表板（先放幾張重要數字卡片）、客戶清單（可以搜尋和篩選）、客戶詳情（分頁切換看聯絡人 / 往來紀錄 / 生意機會）、以及生意機會看板（依進度階段分欄排列）。沒登入要導回登入頁，載入中 / 載入失敗 / 沒有資料時都要有清楚的畫面。請加中文註解。
```

### ④ 把總覽儀表板做成有分析價值的圖表［build］
> 多張經營圖表，點下去能看背後明細
```text
請把總覽儀表板做得更有分析價值，加上這幾張圖表：生意機會在各階段的漏斗圖、未來幾個月的營收預測、各產業的營收分布、客戶風險高低的分布、快到期該續約的客戶、業務的績效排行榜、以及最近的往來活動。而且每一張圖我點下去，都要能看到背後具體是哪些客戶或哪些生意。請加中文註解。
```

### ⑤ 加上「該優先追哪些客戶」的分群［build］
> 依互動近況、頻率、金額自動分級貼標
```text
請在儀表板加一個「該優先追哪些客戶」的功能：依照三個面向自動幫每個客戶評分並貼上標籤——「多久沒聯絡了」、「平常往來頻不頻繁」、「生意金額大不大」。然後給出像「重點客戶」、「有潛力」、「需要喚醒」這樣的分群標籤，讓我一眼就知道該先追誰。請加中文註解。
```

### ✅ 驗證 — 各頁面與圖表都是真實資料［verify］
> 點圖表能下鑽、登出/過期會回登入頁
```text
請幫我做一次完整確認：用測試帳號登入後，總覽的數字、各張分析圖表、客戶清單和詳情顯示的都是後端來的真實資料；點圖表能看到背後的明細；客戶分群標籤有正常顯示；登出或登入過期會回到登入頁。
```

## 實作任務（tasks）
- **u5-t1**：安裝 Node.js 並執行 npm 驗證
- **u5-t2**：使用 Vite 建立 React 19 專案並以 npm install 安裝
- **u5-t3**：配置 vite.config.js 的 server.proxy 代理
- **u5-t4**：套用 CSS 動畫實作骨架屏與毛玻璃視覺效果

## 對應素材（materials）
- {"id":"mat6","type":"MD","name":"React_與_Axios_前後端整合說明","desc":"React 19 呼叫後端 API、JWT 自動攜帶與 Axios 攔截器 (Interceptors) 設計說明。"}