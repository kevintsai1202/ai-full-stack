# u8｜結訓專案衝刺與 Demo Day 驗收

> 整合 Unit 1-7 所有產出，完成一套從登入、客戶管理、AI 對話、風險與情緒洞察、團隊分析到 RAG 知識查詢的完整 AI CRM 系統，並進行展示驗收。

**時數**：13:00 ~ 17:00

**特色**：全端系統整合與除錯、效能調校與上線檢查清單、Demo Day 展示與驗收

## 學習目標
- 整合前後端功能，確認登入、客戶管理、AI 對話、RAG 查詢的完整流程可跑通
- 撰寫端到端測試案例，涵蓋三種典型客戶場景的完整使用路徑
- 建立上線檢查清單，涵蓋安全性、效能、錯誤處理與監控指標
- 執行 pnpm run verify 通過完整全端自動化測試與驗收

## 核心原則
結訓專案的核心目標不是新增功能，而是讓前七個單元的產出真正「串起來」成為一套可展示的產品。重點在於排除整合時的版本衝突、CORS 錯誤、環境差異，並建立可重複執行的驗證流程。

## 概念講解（concepts）

### 結訓整合的三大挑戰（分組：結訓整合挑戰）
把七個單元的產出串成一套可展示的系統，最常遇到三類問題：(1) 環境差異——Docker 容器、JDK 版本、Node.js 版本在不同機器上不一致；(2) 跨層整合——前端 Axios 的 base URL、CORS 設定、JWT token 在 header 的格式，任何一環不對就 401/403；(3) 資料流斷裂——seed data 的客戶 ID 與前端硬編碼的測試 ID 不一致，導致查無資料。解決方式是建立一份「整合檢查清單」，從資料庫、後端、前端逐層驗證。

### 端到端測試案例設計（分組：測試策略）
結訓專案需要涵蓋三種典型客戶場景，對應 Unit 3 seed data 的三種客戶類型：(1) 高價值活躍客戶（APIM）——登入後查看客戶詳情、觸發 AI 對話、AI 能正確引用 RAG 知識庫回答產品問題；(2) 資料不足客戶——AI 應誠實回答「查無相關資料」而非編造；(3) 流失風險客戶（GlobalMart）——AI 摘要能正確反映互動頻率下降的趨勢。每個場景都要從登入開始，走完完整的使用路徑。

### AI 功能的測試策略（分組：測試策略）
AI 功能的測試不能用傳統的「斷言輸出完全相等」方式，因為 LLM 的輸出具有隨機性。正確的策略是分層測試：(1) Java @Tool 方法——用一般單元測試驗證輸入輸出；(2) Prompt 組裝——驗證 system prompt 是否正確帶入客戶資料、是否設定了正確的角色約束；(3) RAG 檢索——驗證 vector store 查詢是否回傳相關文件；(4) 端到端——用 Playwright 模擬使用者操作，驗證 SSE 串流是否正確顯示在前端。

### 上線檢查清單與效能調校（分組：上線準備）
AI CRM 上線前需要檢查：(1) 安全性——JWT secret 是否已替換為生產金鑰、CORS 白名單是否已收斂、API 是否有 rate limiting；(2) 效能——SSE 連線是否有 timeout 設定、RAG 查詢的 topK 與 similarity threshold 是否合理、資料庫連線池大小是否足夠；(3) 可觀測性——是否有結構化日誌、是否記錄 AI 呼叫的 token 消耗與延遲；(4) 錯誤處理——LLM API 不可用時是否有 fallback、前端是否能優雅顯示錯誤訊息。

### Demo Day 展示準備（分組：Demo Day 展示準備）
Demo Day 的展示順序建議：(1) 先展示登入與角色切換，證明 Spring Security + JWT 正常運作；(2) 展示客戶列表與商機管理，證明 JPA + Flyway 的資料層完整；(3) 開啟 AI 助理聊天室，用 SSE 串流展示即時回應；(4) 提問需要 RAG 知識的問題（如退貨政策），展示 AI 引用正確文件回答；(5) 開啟 Swagger UI 展示 OpenAPI 文件。每個步驟預先準備好測試資料，避免現場輸入導致意外。

**CRM 完整流程展示腳本**

建議按以下順序展示 AI CRM 系統：
1. 登入系統（展示 JWT 認證）→ 進入 Dashboard 查看本月指標
2. 進入客戶列表 → 使用動態篩選找到 APIM → 查看客戶詳細資訊
3. 開啟 AI 助理 → 詢問「APIM 最近的合約狀況和商機進度」（展示 Tool Calling）
4. 詢問「GlobalMart 有流失風險嗎？建議怎麼處理？」（展示 RAG + 歷史記憶）
5. AI 產生行動建議 → 確認建立待辦任務（展示完整業務閉環）

## 提示詞（prompts）

### ① 把所有功能整合成可展示的 AI CRM［build］
> 後端 + 網頁 + AI + 知識庫全部串起來
```text
請接續前面所有做好的東西，把後端、網頁畫面、AI 智慧助手、客戶風險與情緒洞察、團隊分析、以及知識庫問答，全部整合成一套可以完整展示的 AI CRM，確認它們能正確一起啟動運作。並針對三種典型客戶——高價值的活躍客戶、資料還很少的客戶、有流失風險的客戶——建立從頭到尾的測試。請加中文註解。
```

### ② 上線前檢查與 Demo 展示準備［build］
> 檢查清單 + 一條龍展示腳本
```text
請幫我整理一份上線前的檢查清單（安全性、效能、出錯時的處理等），並逐項幫我檢查。再準備一套展示腳本，依序串起最有亮點的流程：登入 → 看客戶清單 → 跟 AI 對話 → 看客戶評估與風險 → 看儀表板分析圖表 → 看團隊分析 → 用知識庫做一次文件問答。
```

### ✅ 驗證 — 完整走一次展示流程［verify］
> 從零跑通即通過結訓驗收
```text
請陪我從零完整跑一次驗收：啟動服務、登入、瀏覽客戶、跟 AI 對話並確認它查到的數字是真的、看客戶的風險與情緒、看儀表板的分析圖表、看團隊分析、再用知識庫做一次文件問答，全程沒有錯誤就算通過結訓驗收。
```

## 實作任務（tasks）
- **u8-t1**：完成前後端整合，確認登入、客戶管理、AI 對話的完整流程可跑通
- **u8-t2**：撰寫三個典型客戶場景的端到端測試案例
- **u8-t3**：建立上線檢查清單並逐項確認
- **u8-t4**：完成 Demo Day 展示腳本並試跑驗收