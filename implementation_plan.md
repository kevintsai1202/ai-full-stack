"# 優化教學網頁呈現方式實作計劃\n\n本計劃旨在重構並優化 AI CRM 課程教學網頁的視覺呈現與程式碼架構。我們將運用來自 `modern-web-guidance` 技能的現代 Web 平台 API、更高級的 HSL 配色系統、原生 `<details>` 互斥手風琴 (exclusive accordion) 以及 CSS 滾動驅動動畫 (scroll-driven animations) 等技術，為學員打造極致流暢、精緻 (WOW) 且具備無障礙 (A11y) 原生支援的學習體驗。\n\n## 使用者審查事項\n\n> [!IMPORTANT]\n> **1. 原生 `<details>` 折疊樣式與測試相容性**\n> 我們將把原本由 JavaScript 手動控制顯示隱藏的單元，重構為原生 `<details>`，並搭配 CSS `interpolate-size` (Chrome 129+) 實現高度漸變折疊動畫。此變更會使所有 8 個單元的 `.prompt-box` 持續存在於 DOM 中（但在收合狀態下為不可見或 `inert` 狀態），這不僅能使 Playwright 測試中的數量檢查完美通過，也能提供更好的瀏覽器原生搜尋 (Ctrl+F) 自動展開功能。\n> \n> **2. 全新極致深色模式 (Dark Mode)**\n> 我們將使用現代 CSS 的 `light-dark()` 函式定義變數，讓深色模式與淺色模式的變數定義更集中且平滑。\n\n## 待解決問題\n\n目前沒有待解決問題，我們將直接套用已驗證的現代 Web 技術與精緻視覺設計。\n\n## 預計變更\n\n---\n\n### [前端] 教學網站 (teaching-site)\n\n優化網站的視覺美感、排版結構與互動體驗。\n\n#### [MODIFY] [index.html](file:///d:/GitHub/hahow-ai-full-stack/teaching-site/index.html)\n- 導入 Google Fonts (Inter 與 Outfit)，提升排版專業感。\n- 新增 `<div id=\"scrollProgress\" class=\"scroll-progress\" aria-hidden=\"true\"></div>` 作為頂部閱讀進度指示器。\n- 將原先由 JavaScript 陣列渲染控制的 `.unit` 動態區塊，重構為使用 `<details name=\"course-unit\" class=\"unit\">` 的互斥手風琴架構。\n- 移除所有手動控制展開狀態的 JavaScript 事件，改為監聽原生的 `toggle` 事件
<truncated 2205 bytes>